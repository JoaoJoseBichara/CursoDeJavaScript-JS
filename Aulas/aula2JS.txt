String — Cadeia de caracteres.
Number — São os números, (inteiros (int) e não inteiros (float).
Boolean — (true ou false)


array

function

_______________________________
Declaração de variáveis no JS

var *** obsoleto (permite recriar variáveis com o mesmo nome)
let (melhor)
const
Number pode ser usado para converter uma String em número
Comando number:
Number.isInteger(variável) -> verifica se a variável ou constante é um número inteiro.

.toFixed(2) -> arredondamento casas decimais

Number também pode ser usado para converter uma String em número

***O JS não gera erro na divisão por 0

funções matemáticas
Math.pow(2,3) (exponenciação)
	3
       2
________________________________

typeof = mostra o que tem relacionado ao var
console.log


____________________________


Math.sqrt(9) (raiz quadrada)
Math.PI

_____________________________
Desenvolvimento de um APP para calculo da média de 2 notas:

media = nota1 + nota2
	______________

	      2

variáveis: nota1, nota2, media
*** numéricas (não inteiras)


Entrada : nota1 e nota2
Processamento: media = (nota1 + nota2) / 2
Saída: media